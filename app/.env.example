# The browser only needs the API URL. Never put an administrator token in VITE_* variables.
VITE_AGENT_API_BASE_URL=http://localhost:8787
# Optional local-only preview. Keep false for every production build.
VITE_DEMO_MODE=false
